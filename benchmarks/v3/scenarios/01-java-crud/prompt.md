# Scenario 01: Java CRUD REST API

## Prompt

Create a REST API for managing products with the following requirements:
- CRUD operations (create, read, update, delete)
- Product has: id, name, description, price, category
- Use Spring Boot 3 with Java 21
- Include proper error handling
- Add validation for inputs
- Write tests
