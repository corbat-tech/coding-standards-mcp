# Scenario 06: TypeScript Express CRUD

## Prompt

Create a REST API for managing users with TypeScript and Express:
- CRUD operations
- User has: id, email, name, role, createdAt
- JWT authentication
- Input validation with Zod
- Error handling middleware
- Write tests with Vitest
