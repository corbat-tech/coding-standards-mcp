# Scenario 08: React Form Component

## Prompt

Create a contact form component in React:
- Fields: name, email, message, priority (low/medium/high)
- Client-side validation
- Loading and error states
- Accessible (ARIA labels)
- Write tests with Testing Library
- Use TypeScript
